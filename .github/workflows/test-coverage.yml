name: go-test

on:
  pull_request:
    branches: [ 'main']
  workflow_dispatch:
    branches: [ '**' ]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "FIRST JOB"
      - run: echo "SECOND JOB"
      - run: echo "THIRD JOB"
      - name: Check out repository name
      
